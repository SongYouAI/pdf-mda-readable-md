---
name: pdf-mda-readable-md
description: >-
  把年报 PDF 的「董事长致辞 / 主席報告 / 致股东」与「管理层讨论与分析 / 董事會報告」章节，
  抽取并**重整为可正常阅读的中文结构化 Markdown**（段落合并、标题分层、表格还原、
  港股中英双语去英文栏）。与 `pdf-annual-report-md`（高保真·单位归一·财务数据质控）分工：
  本 skill 面向**定性叙述（MD&A/致辞）的阅读与检索（RAG）**，不追求财务数字的单位归一。
  触发词：年报章节转 md、董事长致辞提取、管理层讨论与分析提取、MD&A 转 markdown、
  港股年报去英文、年报双语去重、年报正文整理、年报段落重组。
agent_created: true
---

# 年报「董事长致辞 + 管理层讨论与分析」→ 可读中文 MD

把一大堆年报 PDF，一次性抽成**每家一份、按年份分段、中文、可读**的 MD。
沉淀自电力行业 P2 研究（20 家公司 / 196 份年报，A股+港股，简繁混排）实战。

## 1. 定位与边界

**做什么**
- 定位章节：`董事长致辞/主席報告/致股東/董事會致辭` + `管理层讨论与分析/董事會報告/業務回顧`。
- 重整为可读 MD：**段落合并 + 标题分层 + 表格还原 + 去英文**。
- 输出：**每家公司目录下，每一年一份独立文件**，统一归置到**专属子文件夹** `董事长致辞与MD&A/`：
  `{NN_公司}/董事长致辞与MD&A/{公司名}_{年份}_董事长致辞与MD&A.md`
  （如 `01-发电运营（15家）/02_华能国际/董事长致辞与MD&A/华能国际_2023_董事长致辞与MD&A.md`）。
  **文件名带公司名前缀 + 年份**（同子文件夹内多份产物并列时自解释；由 `NN_公司` 目录名剥掉序号前缀得到 `公司名`）。
  ⚠️ ① 不合并为单文件——老板明确要求「每一年弄一个 MD 文档」。
  ⚠️ ② **产物必须进专属子文件夹，绝不直接丢在公司根目录**（与 `年报PDF/`、`招股资料/`、理杏仁 CSV 混放会显乱）——老板 2026-09-20 点名纠正。脚本 `SUBDIR` 常量统一控制，20 家一致。
- **默认输出简体（强制）**：抽取后统一经 `opencc` 繁→简转换（`FORCE_SIMPLIFIED`，脚本顶部常量）。港股年报章节常为繁体（「主席報告」「發電量」「裝機容量」），转换后统一为简体，下游简体关键词抽取器才能命中。opencc t2s 幂等，对纯简体文本无副作用；缺依赖时安全降级（仅告警不阻断）。**无需事后手动归一**——这正是 2026-09-22 老板「以后所有内容都要弄成简体」的落地点。

**边界**
- ✅ 文字层 PDF（PyMuPDF/pdfplumber 可提取）。❌ 扫描版/OCR 不做。
- ❌ 不做单位归一（千元/万元→元）——那是 `pdf-annual-report-md` 的职责；本 skill 保原文口径。

**与近邻 skill 区分**：`pdf-annual-report-md` = 高保真财务数据（单位归一 + 乱码修复 + 忠实性 0 幻觉）；
本 skill = 定性叙述的可读化 + 检索友好（去英文、段落化、标题化）。

## 2. 两阶段原则（重要，别只做阶段一）

> 🔴 **老板明令：用年报类 skill 必须走完两阶段。** 阶段一=抽取底稿，**阶段二=结构化重整**。
> 只把 PDF 原始逐行文本倒出来（断行碎片 + 双语英文）不算完成。

- 阶段一（脚本）：`extract_chairman_mda.py` 定位页区（PyMuPDF，已校准）。
- 阶段二（脚本）：`tidy_extract.py` 可读化（pdfplumber）。
- 本 skill 把两阶段都脚本化了，一键产出终稿。

## 3. 快速使用

```bash
PY=/Users/niusl321/.workbuddy/binaries/python/envs/default/bin/python   # 需 pymupdf + pdfplumber
# 全量（目录下每个公司子文件夹一份 MD）
$PY references/extract_chairman_mda.py --base "/path/到/公司根目录"
# 单家 / 只探测页区不落盘
$PY references/extract_chairman_mda.py --base "/path" --company 01_国电电力
$PY references/extract_chairman_mda.py --base "/path" --dry
```
- 目录约定：`<base>/<NN_公司>/年报PDF/<公司>_YYYY年年度报告.pdf`；生成产物落在 `<base>/<NN_公司>/董事长致辞与MD&A/<公司>_YYYY_董事长致辞与MD&A.md`（专属子文件夹，不在根目录）。
- 依赖：`pymupdf`（页区检测）+ `pdfplumber`（可读化）。装于 managed venv。
- `--base` 可覆盖脚本内默认 BASE；脚本会遍历 base 下所有子文件夹，无年报的文件夹只出占位表头。

## 4. 技术要点（踩坑清单，改脚本前必读）

**页区检测（extract_chairman_mda.py）**
1. 🔴 **简繁 `[节節]` 字符类**：regex 写 `节節?` 只匹配简体「节」，港股繁体「第X節」全漏 → 必须 `[节節]`。
2. 🔴 **TOC 跳过**：① A股 `>=2 个不同编号节`；② 标题后跟引导点/纯页码；③ **港股目录是「页码在前」格式**（`10主席報告`），补「短行内含≥4 章节标题，或 ≥3 标题+≥3 独立页码」判目录。否则目录里裸页眉会把起点提前。
3. 🔴 **防重叠**：港股双报告合并扉页（同页标 `主席報告`+`董事會報告`）→ 若 MD&A 起点**严格落在**董事长区间内（`ms < ce`）则从董事长结束页后重搜。`ms == ce` 是章节紧邻，保留。
4. 🔴 **own_num 兜底**：命中行无「第X节」前缀（裸页眉）时，取**本页首个编号节**作本节编号。否则后续同号页眉（如神华「第四節 董事會報告」）会被误判为新边界 → 章节被截断（神华 2023 曾只取到 5 页）。
5. 🟡 结束判定用 `not toks.issubset(section_tokens)`（非 `isdisjoint`）：某页可能同时带已知页眉与新顶级章节。

**可读化（tidy_extract.py）**
6. 🔴 **幻影表（最坑，优先查这条）**：部分港股年报页面含**页外裁切线/装饰框**（线条坐标出现 `x0=-575`、`x1=1170`），pdfplumber 以线框建表 → 造出 bbox `(0,0,1170,893)`（页面仅 595×842）的**整页 10~19 列假表**。后果双重：① 整页文字被 `extract()` 按假列错位切成乱码；② 该 bbox 又从正文词流中排除 → **整页正文丢失**。华润电力 2016 有 289 张全中（越界幅度 575~609pt ≈ 一个页宽；同report 真表 0/289 越界；华能/神华真表 0/460 越界）。
   → 渲染前必须过 `_table_ok(t, page)` 三道判据（任一命中即拒）：**① bbox 越出页面 >5pt**（真表必在 MediaBox 内）；**② ncol<2**（退化单列，交回正文重排）；**③ ncol≥10 且空单元占比≥0.6**（版面网格误判特征）。被拒的表**不剔 bbox**，让文字回落正文重排 + 走双栏去英文。
   ⚠️ 该 bug 极隐蔽：文件仍有大量文字、行数正常，只测「英文残留/乱码」不测「是否整页被表格化」会漏过。
7. 🔴 **去英文（港股双栏）**：中文列与英文列并列，栏间缝隙常仅 ~14pt（固定 gap 阈值不可靠）→ **扫候选切分线取跨线词最少者**（`spans<=2`）；**平衡判据用「两侧各≥8 词」而非比例**——英文被拆成大量短词、中文词少而长，按比例会漏判（华润 p39 英 313/中 29）。保留中文字符占比高的栏；两栏皆中文则都留。
8. 🔴 **表格退化**：`find_tables()` 对复杂港股表常识别成**单列**（整表挤一格，比原文还难读）→ 仅**列数≥2**的表渲染 markdown 并从正文剔除 bbox；**退化单列表不剔，回落正文重排**。
9. 🔴 **反序页眉 / 字符三连叠印 / logo 水印**（页眉旋转 180° 绘制 → pdfplumber 逐字倒读）：`報年5202`=2025年報、`司公限有份股源能色綠天新`=新天綠色能源股份有限公司；同页眉/表头叠印还会致字符**三连**重复（`董董董事事事長長長`）。判据 `_is_reversed_or_garbled`：**短行（≤40）倒读含「有限公司」或「年報+年份数字」**；或**短行（≤30）出现 ≥3 连续同字**。
   ⚠️ **叠字判据必须用 ≥3 连续（`(.)\1{2,}`），绝不可用 ≥2**：合法专名常含 2 连字（`国电电力`/`H股股東`/`國電東北`），用 2 连会把 `国电电力山东新能发电电力源开发有限公司` 当垃圾丢弃（实测全库误伤 11 行、其中含真实子公司名）。实测分离度：≥3 判据精准命中 2 行真伪影（`是担担担保否`），0 误伤。仅对短行生效，避免误伤正文长句。
   🔴 **logo 水印（华润电力类，两个必做动作）**：公司 logo 字母沿曲线排布，pdfplumber 逐字读出，污染两处：
   (a) **中↔英高频交错行**（`o華ur潤ce電s`、`H股股東H股股東`）→ 判据：短行（≤30）**含数字者豁免**，`中↔英` 交替次数 ≥3 且占长度比 ≥0.35。实测全库命中 22 行、**精度 22/22（0 误伤）**。
   (b) **孤立单字符段落成串**（`g s C o m p a n y L i m i t e d`）→ 判据 `_drop_logo_runs`：仅当**连续 ≥4 个单字符段**同现才判装饰（单发/两连可能是真实列标签如 `A`）。实测长度≥4 的 run 全库 25 个、**全部**是华润 logo；长度≤2 的 168 个 run 为散落残留，不动。
   🚨 **关键：去英文必须做「行级 + 段级」两次**。行级过滤挡不住**并段拼接出的英文**——相邻两行分别以 `eti` 结尾、以 `Re dso` 开头，各行都不算英文行而幸存，`join` 后却拼成 `etiRe dso`（华润实测残留 3 处）。故并段后须再按同一判据筛一次。
10. 🟡 **标题分层**：匹配 `第X节 / 一、 / （一）/ 1. / （1）`；**排除以续行标点（，、；：）结尾的伪标题**（否则「一、本公司…真实、」被拆开）。一级 → `####`，二级 → `#####`。
11. 🟡 **页家具**（简繁英同收）：报告标题含年份（`2023 年年度报告`/`2024年報`/`Annual Report`）、页码 `X/Y`、**裸页码**（`9` / `0002`，pdfplumber 常拆成独立行）、**章节名重复行**（正文页眉复述 `主席報告`/`董事會報告`，MD 已有 `###` 标题故冗余）、`单位：`、`√适用□不适用` 勾选框、证券代码、URL、上接/下转、裸 `第X节`、港股导航页眉碎片（管理層/企業管治/其他資料…）。
    → 裸页码规则实测安全：全库 1487 条裸数字行，**0 条**夹在两个长中文段落之间（非正文）。
12. 🔴 **段落分段：缩进为主、行距为辅**（最影响可读性，勿只靠行距）。
    中文年报段落靠**首行缩进 2 字符（≈21pt）**分隔，且**行距常常均匀无突变**。若只用「行距突变（`med+1`）」分段会同时犯两个错：①**段内假断**——行距在 15/18pt 间自然波动（bin 5↔6），阈值 6 使每遇 gap=6 就断段，实测华能国际 2023 **每 ~200 字假断一次**（「…大范围持续高温」∥「和寒潮天气等因素影响…」）；②**真实段断漏掉**——两段 gap 恰好等于常态时并被并为一段。
    → 正确判据：**① 该行 x0 比本栏左边距缩进 ≥12pt → 新段首行（主）**；② 行距 `≥ max(med+3, med*1.5, 4)` → 新段（辅，兜住无缩进版式）；③ 标题单独成段。
    → 🔴 **左边距必须取「正文行 x0 的众数」，绝不能取 min**：港股页脚/页眉（如 `12中國神華能源股份有限公司`，x0=34）比正文（x0=58）更靠左，取 min 会把 margin 拉到 34 → 所有正文行「缩进」都算成 24pt ≥ 12 → **每行都断段**（神华 p13 由 3 段炸成 19 段）。众数代表主导正文左边距，天然抗离群。每栏独立计算，兼容双栏。
    → 另有**跨栏/跨页断段**：`_reflow` 按「页×栏」独立分段，边界落在句中会产生假分段（A股双栏、港股跨页均常见）。故 `_split_columns` 应返回**栏块列表**（而非扁平段落表），调用方以 `(页, 栏序)` 标识各块，当 ①相邻块皆正文段 ②栏块标识不同 ③上段未以句末标点（`。！？；：…"」』）)`）收尾 ④下段非标题 时**并回**。
    → 验收口径：正常段落长度应集中在 **200–500 字**（中文年报一段的典型长度）。若某页出现大量 40–60 字的「段」，即为过度分段，回查 margin 与阈值。
    → 裸页码规则实测安全：全库 1487 条裸数字行，**0 条**夹在两个长中文段落之间（非正文）。
13. 🟡 **纯符号噪声**：`_reflow` 内丢弃**不含任何中/英/数字符**的行（`# — · |` 等残留）。

**已知局限（工具边界，如实标注）**
- 极少数「数据概览 / 图版」页（如神华「運營數據概覽」）为**多表拼版**，pdfplumber default 策略出单列、text 策略 0 表 → 还原不出，文字顺序可能乱。已在输出 MD 头注明「以源 PDF 页码回查」。

## 5. 输出结构

```
# <公司> · <年份> 年年度报告 · 董事长致辞与管理层讨论与分析
> 来源 / 抽取范围（董事长致辞 + 管理层讨论与分析）/ 已做可读化重整 / 只保留中文 / 说明 / 局限
### 董事长致辞（主席報告 / 致股东）
> 抽取范围：第 X–Y 页（命中标题「…」）
<可读正文，标题为 #### / #####>
### 管理层讨论与分析
> 抽取范围：第 X–Y 页（命中标题「…」）
<可读正文>
```
> 一家公司 = 多个年份文件（1 年 1 文件），不再合并为单文件；全部位于该公司 `董事长致辞与MD&A/` 子文件夹内，与公司根目录的 `年报PDF/`、`招股资料/`、理杏仁 CSV 区分。
```
- 未找到的章节**如实标注**「【未找到该章节…】」，绝不虚构（0 幻觉）。

## 6. 质量验收（交付前必过）

用 `references/audit_md_quality.py` 一键批量质检（逐家明细 + 全库合计，0 硬伤才过关）：

```bash
$PY references/audit_md_quality.py --base "/path/到/公司根目录"
```

五项硬伤（任一 >0 即需复核）：
- **英文残留行 = 0**（`≥6 英文词且全行无中文`）。
- **反序页眉 = 0**、**字符重复 = 0**（见 §4-9 判据）。
- **纯符号行 = 0**（⚠️ 审计脚本须白名单 `---` 分隔线、`|---|` 表格分隔行、`| （%） | – |` 类表格数据行，否则会误报数千条）。
- **结构覆盖**：「董事长致辞」命中率 ≈30-40%（A股多数无独立致辞章，属正常）；**MD&A 命中率应接近 100%**。
- 抽查：A股段落合并正确（`公司实现营业`+`收入…` 已并句）、港股仅剩中文、标题已分层。

🚨 **必查「幻影表」回归**（§4-6）：若某家英文行数异常偏高（如华润 121 行），先怀疑整页被幻影表吞掉。
一行自查——统计 MD 中**以 `|` 开头的行占正文比例**：正常 <5%，若某家骤高（如 >20%）即中招。

- 页码越界自查：董事长/MD&A 区间不应吞入「目錄/五年摘要」等前置页（TOC 检测生效）。

## 7. 复用与维护

- 新增公司/年份：放入 `年报PDF/` 后一键重跑即可；脚本幂等覆盖。
- 改脚本后：对同一份 PDF 重跑 `--dry` 对比页区、抽 1-2 页看可读化输出，确认无回归。
